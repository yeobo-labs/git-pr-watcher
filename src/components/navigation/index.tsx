import * as React from 'react';
import styles from './navigation.module.css';

const Navigation = () => (
    <div className={styles.container}>
        <span className={styles.siteName}>GitHub PRoll</span>
    </div>
);

export default Navigation;